package de.hampager.dapnet.service.database;

public class SanitizeUtils {

	/*
	 * // TODO: Make it work static JsonNode removeWhiteSpacefromArray (JsonNode
	 * node) { if (node.isArray()) { String key = node.fieldNames().next(); for (int
	 * i = 0; i < node.size(); i++) { if (node.get(i).isTextual()) { //TODO change
	 * entry i with .replaceAll("\\s+", "")); } }
	 * 
	 * } }
	 */
}
